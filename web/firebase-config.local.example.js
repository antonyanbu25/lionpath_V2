/**
 * Copy to firebase-config.local.js (gitignored) and fill with your Firebase web app config.
 * When projectId is set, the portal uses Google SSO + Firestore instead of dummy login.
 */
export const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-firebase-project-id",
  appId: "1:123456789:web:abc123",
};
