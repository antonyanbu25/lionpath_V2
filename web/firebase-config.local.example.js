/**
 * Copy to firebase-config.local.js and fill from Firebase Console:
 * Project settings → Your apps → Web app → firebaseConfig
 *
 * firebase-config.local.js is gitignored — include it in VPS deploy zip.
 */
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "se-singha-paathi.firebaseapp.com",
  projectId: "se-singha-paathi",
  appId: "YOUR_APP_ID",
};
